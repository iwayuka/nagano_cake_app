<h1>商品新規登録</h1>
<%= form_with model:@item, url:'/admin/items', local:true do |f| %>
  <h4>商品画像</h4>
  <%= f.attachment_field :image %>
  <h4>商品名</h4>
  <%= f.text_field :name, placeholder: "商品名" %>
  <h4>商品説明</h4>
  <%= f.text_area :introduction, placeholder: "ここに説明文を記述します" %>
  <h4>ジャンル</h4>
  <!--Genresのデータベースからカラムを引っ張って、プルダウンを実装するためのコード -->
  <%= f.collection_select :genre_id, Genre.all, :id, :name, include_blank: "-- 選択して下さい --" %>
  <h4>税抜価格</h4>
  <%= f.text_field :price, placeholder: "1000" %><p>円</p>
  <h4>販売ステータス</h4>
  <%= f.label :is_activea, '販売中', value: 1 %>
  <%= f.radio_button :is_activea, '1' %>
  <%= f.label :is_activea, '販売停止中', value: 0 %>
  <%= f.radio_button :is_activea, '0' %>
  <%= f.submit '新規登録' %>
<% end %>