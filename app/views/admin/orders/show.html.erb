<h2>注文履歴詳細</h2>

<table>
  <tr>
    <th>購入者</th>
    <td><%= @order.customer.last_name %> <%= @order.customer.first_name %></td>
  </tr>
  <tr>
    <th>注文日</th>
    <td><%= @order.created_at %></td>
  </tr>
  <tr>
    <th>配送先</th>
    <td><%= @order.customer.postal_code.to_s.insert(3, "-") %><%= @order.customer.address %></td>
  </tr>
  <tr>
    <th>支払方法</th>
    <td><%= @order.payment_method %></td>
  </tr>
  <tr>
    <th>注文ステータス</th><!-- 注文履歴詳細画面内で、ステータスを変更・更新できるようにする -->
    <td><%= @order.status %></td>
  </tr>
</table>

  <table>
    <tr>
      <th>商品名</th>
      <th>単価（税込）</th>
      <th>数量</th>
      <th>小計</th>
      <th>制作ステータス</th>
      <th></th>
    </tr>
    <% @order_details.each do |order_detail| %>
    <tr>
      <td>
        <%= order_detail.item.name %>
      </td>
      <td>
        <%= order_detail.price %>
      </td>
      <td>
        <%= order_detail.amount %>
      </td>
      <td>
        <!-- 小計の挿入 -->
      </td>
      <td><!-- 後ほど注文詳細履歴で変更・更新 -->
        <%= order_detail.making_status %>
      </td>
    </tr>
    <% end %>
  </table>

  <table>
    <tr>
      <th>商品合計</th>
      <td><!--小計の合計 --></td>
    </tr>
    <tr>
      <th>送料</th>
      <td><%= @order.shipping_cost %></td>
    </tr>
    <tr>
      <th>請求合計金額</th>
      <td><!--「商品合計＋送料」を計算して出力 --></td>
    </tr>
  </table>


